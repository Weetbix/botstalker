  <a class="image" href="#channel/{{ token }}/{{ id }}">
    <img src="{{ user.attributes.profile.image_192 }}">
  </a>
  <div class="content">
    <div class="header">{{ user.attributes.name }}</div>
    <div class="meta">
    <span>
      <i class="mail icon"></i>
      {{ message_count }}
    </span>
    </div>
  </div>